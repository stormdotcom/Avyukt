How to run the setup file?? 

First, Run these 3 commands before running the setup file 

`sudo apt-get update`
`python -m pip install -r requirements.txt`

Now, run the setup file using this command `python ./setup.py`
Setup will start on it's on, Just wait for somtime and it will be completed,
After the setip gets completed, You are ready to go! You can run the Framework
